zabbix_rpm = node['zabbix_rpm']
remote_file "#{Chef::Config[:file_cache_path]}/#{zabbix_rpm}" do
  source "http://repo.zabbix.com/zabbix/3.0/rhel/6/x86_64/#{zabbix_rpm}"
  action :create
end

rpm_package "zabbix3.0" do
  source "#{Chef::Config[:file_cache_path]}/zabbix-release-3.0-1.el6.noarch.rpm"
  action :install
end
