#### ECR

##### コンテナのビルド
```
$(aws ecr get-login --no-include-email)
docker build -t ${DEV_ECR_REPOSITORY}:base .
docker tag ${DEV_ECR_REPOSITORY}:base ${DEV_ECR_REP_URL}:base
docker push ${DEV_ECR_REP_URL}:base
```
