case node['type']
  when 'app' then
    include_recipe 'ruby_platform::opsw_deploy_rails'
  when 'job' then
    include_recipe 'ruby_platform::opsw_deploy_rails_job'
  when 'api' then
    include_recipe 'ruby_platform::opsw_deploy_rails_api'
  else
    Chef::Log.info("********** There is no type for deployment **********")
end
